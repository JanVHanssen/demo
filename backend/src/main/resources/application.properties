# JWT Configuration
jwt.secret=abcdefghijklmnopqrstuvwxyz1234567890
jwt.expiration=3600000

# Application Configuration
spring.application.name=backend

# ✅ Database configuration (Neon / PostgreSQL via JDBC)
spring.datasource.url=jdbc:postgresql://ep-still-boat-a21diwi6-pooler.eu-central-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_BY81iSLREHjd
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration 
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ✅ Flyway Configuration - AANGEPAST VOOR CLEAN START
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.clean-on-validation-error=true
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=0

# Logging Configuration
logging.level.root=WARN
logging.level.be.ucll.se.demo=DEBUG
logging.level.org.flywaydb=DEBUG